# AI Novel Studio UI 精致度提升方案（2026-08-01）

> 背景：用户反馈"功能强但第一眼不够惊艳"，目标对标 Sudowrite/NovelAI 的打磨程度。
> 方法：加载前端设计 skills（frontend-ui-engineering / web-design-engineer style-recipes）+ 联网调研（2026 UI 趋势 / Sudowrite-NovelAI 对比）+ 当前 UI 数值审计。
> 参照锚点：**Linear**（暖黑发丝边框克制派）+ **Raycast**（玻璃拟态键盘优先派）——两者是 2026 暗色生产力工具的两种标杆。

---

## 一、诊断结论

### 1.1 现状审计（style.css 实测数值）

| 维度 | 当前值 | 判定 |
|---|---|---|
| 背景 | `#0e1018`（偏蓝黑） | 中性偏冷，缺暖调深度 |
| 面板 3 级 | `#161a28 / #1d2230 / #252b3d` | 结构正确 |
| 边框 | `#2a3042`（可见灰线） | **过重** — 最明显 AI 味 |
| accent | `#f0a060` 橙 | 使用区域过大 |
| 圆角 | 10px / 7px | 缺 6px 小控件档 |
| 阴影 | 多重 + 4px/8px/16px 模糊 | **过重** — AI 味特征 |
| 字体 | Segoe UI 15px | 无 display/正文层级 |
| 间距 | 未统一刻度 | 待治理 |

### 1.2 根因（对照 frontend-ui-engineering skill 反 AI 味清单）

1. **阴影过重**：`shadow = 0 1px 3px + 0 4px 16px`、`shadow-lg = 0 8px 40px` —— 多层阴影堆叠是 AI 生成 UI 的典型特征，且拖慢低端机渲染。
2. **边框过重**：`#2a3042` 实色边框替代发丝线（Linear 用 `rgba(255,255,255,0.06)`），面板间缺乏"轻分隔"。
3. **accent 过量**：橙色出现在 body 背景/大面积按钮上（Linear 原则：accent 只占 <5% 像素，用于焦点/激活态）。
4. **圆角策略平**：10px 一刀切，无"小控件 6 / 卡片 12 / 大面板 16"的层级。
5. **字体层级缺失**：Segoe UI 单一字体，无 display 加权标题（Linear 用 Inter Tight 600 + letter-spacing -0.02em）。

---

## 二、提升方案（Redesign · Preserve 模式：保留三栏布局 + 橙色品牌，精修 tokens）

### 2.1 色彩系统（CSS 变量级改动，不动 HTML 结构）

```css
/* 暗色主题（style.css 或 themes.css 的 dark 段） */
--bg: #0d0e13;            /* 暖黑（原 #0e1018 微调） */
--panel: #14151c;         /* 表面 1 */
--panel2: #1a1b24;        /* 表面 2 */
--panel3: #21222d;        /* 表面 3（raised） */
--border: rgba(255,255,255,0.07);   /* 发丝级边框（原 #2a3042 实色） */
--border2: rgba(255,255,255,0.12);  /* 强调边框 */
--text: #f2f3f7;          /* 主文字 */
--muted: #9aa0b8;         /* 保留 */
--faint: #6b7280;         /* 弱化文字（新增） */
--accent: #f0a060;        /* 保留品牌橙 */
--accent-soft: rgba(240,160,96,0.10);  /* accent 着色的 soft 背景 */

/* 阴影全面减淡（消灭多层堆叠） */
--shadow: 0 1px 2px rgba(0,0,0,0.3);              /* 仅 raised 表面 */
--shadow-lg: 0 8px 24px rgba(0,0,0,0.35);         /* 弹窗/面板 */
--hover-shadow: 0 2px 6px rgba(0,0,0,0.35);       /* hover 微抬升 */

/* 圆角分级（6/12/16 三级，废除 7/10 一刀切） */
--radius-sm: 6px;         /* 按钮/输入/小控件 */
--radius: 12px;           /* 卡片 */
--radius-lg: 16px;        /* 大面板/弹窗 */
```

### 2.2 字体系统

```css
/* 标题（display）：加粗 + 收紧字距，告别 AI 味默认 */
font-family: 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
/* 建议引入本地无依赖方案：系统字体加权 */
h1,h2,.panel-title { font-weight: 600; letter-spacing: -0.02em; }
body { font-size: 14px; line-height: 1.55; }   /* 15→14，更紧凑专业 */
/* 数字/快捷键 chip 用等宽 */
code, .kbd, .shortcut { font-family: 'Cascadia Code', Consolas, monospace; }
```

### 2.3 动效系统（微交互，60fps）

```css
/* 统一缓动：快而克制（Linear 风格） */
:root { --ease-out: cubic-bezier(0.22, 1, 0.36, 1); --dur: 150ms; }
.btn, .nav-link, .tool-btn { transition: background var(--dur) var(--ease-out), 
  color var(--dur) var(--ease-out), transform var(--dur) var(--ease-out); }
/* hover 微抬升（Raycast 式） */
.btn-primary:hover { transform: translateY(-1px); }
/* 主题切换过渡（渐变平滑） */
body, .panel, .editor-pane { transition: background 200ms var(--ease-out), 
  border-color 200ms var(--ease-out); }
```

### 2.4 accent 克制策略（Linear 原则：<5% 像素）

1. body 背景**不用** accent：橙色只保留在 ①激活/焦点态 ②主 CTA ③进度条/状态点。
2. 按钮分层：主按钮保留橙色渐变，次按钮改 `rgba(255,255,255,0.06)` 半透明（Raycast 式），幽灵按钮保留。
3. accent-glow 从 0.5 降至 0.25，消除"发光"感。
4. 新增 accent 弱化态 `--accent-soft` 用于选中背景（如当前章节高亮）。

### 2.5 布局细节（低成本高感知）

1. **面板分隔**：所有面板用发丝边框 + 极淡阴影替代粗边框；删除不必要的 box-shadow 嵌套。
2. **焦点态**：键盘焦点用 2px accent 描边（`outline: 2px solid var(--accent); outline-offset: 1px`），兼顾无障碍。
3. **快捷键 chip**：工具按钮/菜单项旁加等宽字体快捷键提示（Raycast 签名式，本项目键盘优先度高）。
4. **空状态**：沿用插画，但加统一的三态文案（空/加载/错误）——frontend-ui-engineering 强调的检查项。
5. **加载态**：生成进度条已很完善，补充骨架屏（章节列表加载时）。

### 2.6 浅色主题同步

亮色主题同规则反相：`--border: rgba(0,0,0,0.08)`、`--shadow` 更淡、accent 保持 #e8945a 但同样克制。

---

## 三、实施顺序（按 ROI）

| 步骤 | 内容 | 预估 | 收益 |
|:--:|---|:--:|---|
| 1 | CSS 变量重写（色板/边框/阴影/圆角/字体/动效） | 2-3h | **最大感知提升**，一次性消除三大 AI 味 |
| 2 | accent 克制（按钮分层 + glow 降低 + soft 态） | 1h | 高级感来源 |
| 3 | 焦点态 + 快捷键 chip | 1h | 专业度 + 无障碍 |
| 4 | 骨架屏 + 三态完善 | 1-2h | 完成度 |
| 5 | 6 主题逐一过一遍（变量联动已修好，验证各主题） | 1h | 防回归 |

**风险控制**：全部为 CSS 变量级改动，不碰 HTML 结构/JS 逻辑；改前备份 style.css + themes.css；用识图 skill 改前改后截图对照验证。

---

## 四、参照数据来源

1. Linear 配方：`~/.config/.../SKILLs/前端skill/garden-skills/skills/web-design-engineer/references/style-recipes/linear.md`（暖黑 #08090A / 发丝边框 / ≤16 圆角 / 150ms 动效 / accent <5%）
2. Raycast 配方：同目录 `raycast.md`（玻璃拟态 / 快捷键 chip / 半透明按钮）
3. frontend-ui-engineering skill：反 AI 味清单（阴影过重/过度圆角/大留白/紫色渐变）
4. 2026 UI 趋势（Snixrs 2026-01）：暗色用深灰非纯黑、玻璃拟态克制化、微交互动效、可变字体、无障碍优先
5. Sudowrite vs NovelAI 对比（TheToolsVerse 2026-06）：Sudowrite 差异化="clean, distraction-free interface"（干净无干扰）

---

*方案由 AI Novel Studio 设计分析 Agent 生成 · 2026-08-01 01:40 CST*
