# AI Novel Studio 前端精修方案（2026-08-01）

> 依据：`多主题UI深度调研报告_2026-08-01.md`（7 主题识图评审 + 前端 skill 原则对照）+ 本方案对 `web/css/style.css`、`web/css/themes.css` 的实码核对（变量均已定位到行号）
> 结论：**不需要美化（大改），需要精修（P0 六项 CSS 变量级调整）**。三栏布局、氛围插画、数据反馈、AI 功能整合是识图评审一致认可的部分，保持不动。

---

## 一、诊断摘要（7 主题共性，按严重度）

| # | 问题 | 实码证据 | 严重度 |
|---|------|---------|:--:|
| C1 | 边框/图标对比度不足 | dark `--border:#2a3042`、`--faint:#78809a`（图标用 faint，深底上 ~3:1） | 中 |
| C2 | 信息密度高 | 左侧 6+ 导航 + 底部输入框 + 生成按钮并列；gold 菜单最多 | 中高 |
| C3 | 部分主题边框实色粗线 | gold `--border:#333333`、dusk `--border:#4a4036` | 中 |
| C4 | 编辑区偏窄 | `.editor-inner{max-width:760px}`（style.css:323） | 低中 |
| C5 | 操作路径冗余 | 人物卡无直接编辑入口、创建需两步 | 低中 |

---

## 二、P0 全局精修（CSS 变量级，一次改完 7 主题全覆盖）

全部改动集中在 `style.css` 顶部 `:root`/`[data-theme]` 变量段 + `themes.css` 各主题变量段，不碰组件结构。

### P0-1 发丝级边框（治 C1/C3）

**原则**：深色主题边框用 `rgba(255,255,255,.06)`，浅色用 `rgba(0,0,0,.08)`；去掉实色 `#333` 类粗线。

| 位置 | 旧值 | 新值 |
|------|------|------|
| themes.css gold `--border`（约 L122） | `#333333` | `rgba(255,255,255,.08)` |
| themes.css gold `--border2`（约 L123） | `#444444` | `rgba(255,255,255,.12)` |
| themes.css dusk `--border`（约 L420 区） | `#4a4036` | `rgba(255,255,255,.08)` |
| themes.css ink-study `--border`（约 L17） | `#d4c5a3` | `rgba(90,60,20,.25)` |
| style.css dark `--border`（L51） | `#2a3042` | `rgba(255,255,255,.07)` |
| style.css dark `--border2`（L52） | `#363d54` | `rgba(255,255,255,.12)` |
| style.css light `--border`（L11） | `#e8e2d8` | `rgba(0,0,0,.08)` |
| style.css light `--border2`（L12） | `#d5cec4` | `rgba(0,0,0,.12)` |

**注意**：`--border` 还被用于分割线（如 `.quota-bar`、`.topbar` 下边框），rgba 发丝线在浅色上会显淡，需保留一个 `--border-strong`（原 border2 值）给分割线场景，验证时逐屏看。

### P0-2 圆角分级（治"一刀切"）

**原则**：6（按钮/小控件）/ 12（卡片/输入框）/ 16（大面板/弹窗），废除 7/10 一刀切。

| 位置 | 旧值 | 新值 |
|------|------|------|
| style.css `--radius-sm`（L34） | `7px` | `6px` |
| style.css `--radius`（L33） | `10px` | `12px` |
| 新增 `--radius-lg` | 无 | `16px` |

随后把弹窗/大面板（themes.css L242/254 的 `border-radius:12px`、L474 的 `14px`，及 style.css 各弹窗类）统一改为 `var(--radius-lg)`；小按钮/输入框保持 `var(--radius-sm)`。

### P0-3 阴影减淡（治多层堆叠）

**原则**：只保留一层极淡投影，消灭 `0 1px 3px + 0 4px 16px` 堆叠。

| 位置 | 旧值 | 新值 |
|------|------|------|
| style.css light `--shadow`（L35） | `0 1px 3px rgba(60,40,20,.06), 0 4px 16px rgba(60,40,20,.04)` | `0 1px 2px rgba(0,0,0,.05)` |
| style.css dark `--shadow`（L71） | `0 1px 3px rgba(0,0,0,.4), 0 4px 16px rgba(0,0,0,.3)` | `0 1px 2px rgba(0,0,0,.3)` |
| themes.css 各主题 `--shadow-sm/md`（ink-study L39-40、gold L144-145、focus L203-204 等） | 双层/多层 | 单层 `0 1px 2px` 同级 |
| 侧栏投影（style.css L166/170、themes.css L64/160/230/234） | `4px 0 24px` 系 | `2px 0 12px rgba(0,0,0,.18)`（深色）/ `.05`（浅色） |

### P0-4 accent 克制（治橙色用太满）

**原则**：accent 只用于焦点/激活/CTA/链接，body 背景与大面积区域不直接用 accent；检查 `.quota-bar` 背景渐变（style.css L92 `linear-gradient(135deg, var(--panel), var(--accent-soft))`）这类大面积 accent-soft 使用。

| 动作 | 位置 |
|------|------|
| 保留：按钮 primary、激活项、focus 环、进度条、字数高亮 | 不动 |
| 弱化：quota-bar 渐变底色 → 纯 `var(--panel)` + 底部 1px accent 细条 | style.css L92 |
| 弱化：`--accent-soft` 大面积背景使用改为 rgba 更低（如 .13→.08） | themes.css 各主题 |

### P0-5 字体层级

**原则**：标题/导航字重 600 + `letter-spacing:-0.02em`；正文保持 13.5-15px。

| 动作 | 位置 |
|------|------|
| `.side-label`（style.css L209）：`letter-spacing:.6px` → 保留但改 `font-weight:700` | style.css |
| `.quota-bar .brand`（L94）、`.app-name`（L192）、`.doc-title`（L286）已 600 → 补 `letter-spacing:-0.02em` | style.css |
| 正文不动 | — |

### P0-6 图标对比度（治 C1 图标看不清）

**原则**：深色主题的图标/次要文字色从 faint（~#78-7a）提到 #9aa0b8 级，保证非文本对比 ≥3:1。

| 位置 | 旧值 | 新值 |
|------|------|------|
| style.css dark `--faint`（L56） | `#78809a` | `#8d95ad` |
| themes.css gold `--faint`（约 L127） | `#666666` | `#8a8a8a` |
| themes.css focus `--faint`（约 L186） | `#6d7484` | `#868ea4` |
| themes.css dusk `--faint`（约 L421） | `#7a6d5d` | `#948675` |

---

## 三、P1 主题个性（P0 完成后再做，可单独取舍）

| # | 项 | 改动 | 主题 |
|---|----|------|:--:|
| P1-1 | 玻璃态增强 | focus `--panel` 透明度 0.86→0.7 + `backdrop-filter:blur(20px)`（需给 `.editor-pane`/`.composer` 加 blur，注意滚动条穿透） | focus |
| P1-2 | 编辑区加宽 | `.editor-inner` max-width 760px→960px（可选 880 保守值），同步调行距 | 全部 |
| P1-3 | 快捷键 chip | 工具栏按钮加等宽字体快捷键提示（Ctrl+Enter/Ctrl+S 已有，补到去AI味/续写/思考） | 全部 |
| P1-4 | 信息密度治理 | 仪表盘/AI工具箱菜单项可折叠（group-head 折叠已有先例，sidebar.js 复用） | gold 最需 |

---

## 四、P2 差异化（强化记忆点，非标准化）

| # | 项 | 改动 | 主题 |
|---|----|------|:--:|
| P2-1 | 纸张质感 | paper-ink 加微噪点纹理（内联 SVG data-uri，类似现有 --empty-art 做法）+ 首行缩进 2em | paper-ink |
| P2-2 | 书斋签名 | ink-study 验证明暗断层（侧深 #241207 vs 主浅 #f5eddb），若突兀则在主区顶部加 1px 渐变过渡带 | ink-study |
| P2-3 | 切换过渡 | `[data-theme]` 切换加 150ms 背景/文字过渡（`transition:background .15s,color .15s`），注意只对 body/panel 级生效避免全局卡顿 | 全部 |

---

## 五、不改清单（识图评审一致认可）

三栏布局、编辑器氛围水印插画（--empty-art 系统）、字数/Token/自动保存数据反馈、AI 功能工具栏整合、7 主题整体配色方向。

---

## 六、实施步骤与验证

1. 备份：`style.css`、`themes.css` 各存一份 `.bak`（或 git commit 后改，本仓库有 git，改前 commit 即可回滚）。
2. 改 P0 六项 → `go build -o server.exe ./cmd/server` → 重启 server（**前端 go:embed 编译进 exe，必须重编译才生效**，历史教训）。
3. 验证：CDP 实机截图 7 主题（`.cowork-temp/theme_*.jpg`）→ 识图 skill（MiMo qwen3-vl-flash）对比三项：边框对比度 ≥3:1、阴影单层、圆角分级；逐屏检查分割线（发丝线是否过淡）。
4. P1/P2 按用户取舍逐项做，每项同样走 编译→截图→识图 循环。
5. 全部通过后：更新发布包（`release/` 目录 config + 打包），git commit。

## 七、风险与回滚

- **发丝线风险**：浅色主题上 `rgba(0,0,0,.08)` 可能过淡，预留 `--border-strong` 给分割线场景；验证阶段若某屏看不清即回退该处。
- **圆角风险**：`--radius` 10→12 影响面广，若某组件突兀，单独覆盖该组件。
- **回滚**：git revert 或恢复 .bak，重新编译即可。
- **工作量**：P0 约 2-3h（含验证）；P1 每项 0.5-1h；P2 每项 0.5-1h。
